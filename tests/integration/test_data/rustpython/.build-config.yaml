environment_variables:
- name: PIP_FIND_LINKS
  value: ${output_dir}/deps/pip
- name: PIP_NO_INDEX
  value: 'true'
project_files:
- abspath: ${test_case_tmp_path}/../test_e2e_cargo_rustpython_0/cachi2-output/deps/pip/cryptography-44.0.2/.cargo/config.toml
  template: |
    [source.crates-io]
    replace-with = "vendored-sources"

    [source.vendored-sources]
    directory = "${output_dir}/deps/cargo"
- abspath: ${test_case_tmp_path}/../test_e2e_cargo_rustpython_0/cachi2-output/deps/pip/maturin-1.8.3/.cargo/config.toml
  template: |
    [source.crates-io]
    replace-with = "vendored-sources"

    [source.vendored-sources]
    directory = "${output_dir}/deps/cargo"
